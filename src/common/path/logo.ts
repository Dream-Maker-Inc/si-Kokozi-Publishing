const PATH = 'kokozi/assets/logo/'

const logoPaths = {
	coloredImage: require(PATH + 'colored-image.png'),
	image: require(PATH + 'image.png'),
	text: require(PATH + 'text.png')
}

export default logoPaths